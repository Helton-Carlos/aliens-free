export interface INotification {
  title: string;
  context: string;
  arrow: boolean;
}

export interface IInfo {
  code: string;
  codein: string;
  name: string;
  high: string;
  low: string;
  image: string;
  ask: string;
  timestamp: string;
  create_date: string;
}
